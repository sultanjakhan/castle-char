# 🔧 Настройка доступа к Vercel API

## Что нужно для автоматизации

Чтобы я мог управлять Vercel проектом (добавлять переменные окружения, деплоить и т.д.), мне нужны следующие данные:

### 1. Vercel API Token

**Как получить:**
1. Зайди на https://vercel.com/account/tokens
2. Нажми **"Create Token"**
3. Дай название (например, "Castle Char MCP")
4. Выбери scope: **Full Account** (или только нужные проекты)
5. Скопируй токен (он показывается только один раз!)

**Формат:** `vercel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 2. Project ID

**Как найти:**
1. Зайди в проект на Vercel
2. Перейди в **Settings** → **General**
3. Найди **Project ID** (или в URL проекта: `vercel.com/your-team/project-name/settings`)

**Формат:** `prj_xxxxxxxxxxxxxxxxxxxxx`

### 3. Organization ID (опционально, но полезно)

**Как найти:**
1. Зайди в настройки команды/организации
2. В URL будет что-то вроде: `vercel.com/team/xxxxx`
3. Или в **Settings** → **General** проекта

**Формат:** `team_xxxxxxxxxxxxxxxxxxxxx` или просто строка (например, `your-team-name`)

## Как передать мне эти данные

### Вариант 1: Через переменные окружения (безопасно)

Создай файл `.env.vercel` (не коммить в git!):

```env
VERCEL_TOKEN=vercel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
VERCEL_PROJECT_ID=prj_xxxxxxxxxxxxxxxxxxxxx
VERCEL_ORG_ID=your-team-name
```

Или добавь их в существующий `.env.local`:

```env
# Supabase (уже есть)
VITE_SUPABASE_URL=https://vadanzerfgwdzktdytdr.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_vItgB5KxmoefzNJ7Y2VIwA_EeIgmBtE

# Vercel API (для автоматизации)
VERCEL_TOKEN=vercel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
VERCEL_PROJECT_ID=prj_xxxxxxxxxxxxxxxxxxxxx
VERCEL_ORG_ID=your-team-name
```

### Вариант 2: Просто скажи мне в чате

Можешь просто написать:
```
Vercel Token: vercel_xxx...
Project ID: prj_xxx...
Org ID: xxx...
```

Я сохраню их локально и буду использовать для управления проектом.

## Что я смогу делать с этими данными

✅ Добавлять/изменять переменные окружения  
✅ Проверять статус деплоев  
✅ Запускать новые деплои  
✅ Просматривать логи  
✅ Управлять доменами  

## Безопасность

⚠️ **Важно:** 
- Никогда не коммить токены в git!
- Токены дают полный доступ к твоему аккаунту Vercel
- Если токен скомпрометирован, сразу удали его и создай новый

## Альтернатива: Vercel CLI

Если не хочешь давать API токен, можешь использовать Vercel CLI:

```bash
vercel login
vercel link  # свяжет проект
```

Но для автоматизации через меня все равно нужен токен.

