# 🗄️ Настройка Supabase для Castle Character Ranker

## Шаги для подключения базы данных

### 1. Создай проект на Supabase

1. Перейди на https://supabase.com
2. Войди или зарегистрируйся
3. Нажми **"New Project"**
4. Заполни:
   - **Name**: `castle-character-ranker` (или любое другое)
   - **Database Password**: придумай надежный пароль (сохрани его!)
   - **Region**: выбери ближайший
5. Нажми **"Create new project"**
6. Подожди 2-3 минуты, пока проект создается

### 2. Создай таблицы в базе данных

1. В проекте Supabase перейди в **SQL Editor** (левое меню)
2. Нажми **"New query"**
3. Скопируй весь код из файла `supabase/schema.sql` в редактор
4. Нажми **"Run"** (или `Ctrl/Cmd + Enter`)
5. Должно появиться сообщение "Success. No rows returned"

### 3. Получи API ключи

1. В проекте Supabase перейди в **Settings** → **API**
2. Скопируй:
   - **Project URL** (это `VITE_SUPABASE_URL`)
   - **anon public** ключ (это `VITE_SUPABASE_ANON_KEY`)

### 4. Настрой переменные окружения

#### Для локальной разработки:

Создай файл `.env.local` в корне проекта:

```env
VITE_SUPABASE_URL=твой_project_url
VITE_SUPABASE_ANON_KEY=твой_anon_key
```

#### Для Vercel:

1. Зайди в настройки проекта на Vercel
2. Перейди в **Settings** → **Environment Variables**
3. Добавь две переменные:
   - `VITE_SUPABASE_URL` = твой Project URL
   - `VITE_SUPABASE_ANON_KEY` = твой anon public key
4. Нажми **"Save"**
5. Перезапусти деплой (или сделай новый push)

### 5. Проверь работу

1. Запусти проект локально: `npm run dev`
2. Открой приложение в браузере
3. Попробуй добавить персонажа или проголосовать
4. Проверь в Supabase → **Table Editor**, что данные появились в таблице `characters`

## 🔒 Безопасность

По умолчанию настроены политики Row Level Security (RLS), которые позволяют:
- ✅ Читать данные всем (публичный доступ)
- ✅ Писать данные всем (для простоты)

**Для продакшена рекомендуется:**
- Ограничить запись только для авторизованных пользователей
- Настроить более строгие политики безопасности

## 📊 Структура базы данных

- **characters** - все персонажи с их рейтингами
- **match_history** - история всех матчей
- **organization_metadata** - описания организаций/фракций

## 🔄 Миграция данных

Если у тебя уже есть данные в localStorage:
1. Экспортируй данные через кнопку "Backup JSON" в приложении
2. После подключения Supabase данные автоматически инициализируются из `constants.ts`
3. Или можно импортировать старые данные вручную через SQL Editor

## ❓ Проблемы?

- **Данные не сохраняются?** Проверь, что переменные окружения установлены правильно
- **Ошибки при создании таблиц?** Убедись, что скопировал весь SQL код из `schema.sql`
- **Не работает на Vercel?** Проверь, что добавил переменные окружения в настройках проекта

