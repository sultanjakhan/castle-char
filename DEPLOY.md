# 🚀 Инструкция по деплою на Vercel

## Автоматический деплой (рекомендуется)

Самый простой способ - подключить репозиторий к Vercel через веб-интерфейс:

1. Перейди на https://vercel.com
2. Войди через GitHub (если еще не вошел)
3. Нажми "Add New Project"
4. Выбери репозиторий `sultanjakhan/castle-char`
5. Vercel автоматически определит настройки (Vite)
6. Нажми "Deploy"

Готово! Твой сайт будет доступен по адресу типа `castle-char-xxx.vercel.app`

## Через Vercel CLI

Если хочешь задеплоить через командную строку:

```bash
# Войди в Vercel (откроется браузер)
vercel login

# Задеплой проект
vercel --prod
```

## Переменные окружения

Если нужен GEMINI_API_KEY, добавь его в настройках проекта на Vercel:
1. Зайди в настройки проекта на Vercel
2. Перейди в "Environment Variables"
3. Добавь `GEMINI_API_KEY` со значением твоего API ключа

## Автоматический деплой через GitHub Actions

GitHub Action уже настроен в `.github/workflows/deploy.yml`. 

Для работы нужно добавить секреты в GitHub:
1. Зайди в настройки репозитория: Settings → Secrets and variables → Actions
2. Добавь секреты:
   - `VERCEL_TOKEN` - получи на https://vercel.com/account/tokens
   - `VERCEL_ORG_ID` - найди в настройках команды на Vercel
   - `VERCEL_PROJECT_ID` - найди в настройках проекта на Vercel

После этого каждый push в main будет автоматически деплоить проект.

